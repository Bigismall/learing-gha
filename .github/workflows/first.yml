name: First workflow
on: push
jobs:
    job1:
        name: First job 
        runs-on: ubuntu-latest
        steps:
            - name: First step
              run: echo "Hello world"
            - name: Second step
              run: env | sort


